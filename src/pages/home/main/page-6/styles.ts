import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    wrapper: {
        background: 'yellow',
        minHeight: '100vh',
        minWidth: '100vw',
        scrollMarginTop: '70px',
    }
});